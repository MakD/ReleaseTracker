# Discord Release Tracker Bot

A Discord bot that monitors GitHub, GitLab, and Docker Hub for new releases and image updates. 

## Installation (Docker)
1. Clone this repository.
2. Copy `.env.example` to `.env` and add your tokens.
3. Copy `repos.example.json` to `repos.json` and add your links.
4. Run `docker compose up -d --build`
